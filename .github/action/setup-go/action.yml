name: setup-go
description: Set up Go from the workspace's go.work directive, caching every module's downloads
runs:
  using: composite
  steps:
    - uses: actions/setup-go@v5
      with:
        go-version-file: go.work
        cache-dependency-path: '**/go.sum'
